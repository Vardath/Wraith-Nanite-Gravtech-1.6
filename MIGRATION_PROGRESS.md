# WNG reconstruction migration

The public `main` branch was intentionally reset to an empty tree before migration.

Migration source: the isolated reconstructed project under the private WNG repository, checked against later private build/package evidence and the complete WNG project requirements. The obsolete original source tree is not a runtime or build dependency of this reconstruction.

Locked corrections during migration:

- Replicator split-born recombination cooldown: 2,500 ticks (one in-game hour).
- A Child's Toy that goes feral transforms into a Replicator and enters the Replicator threat loop.
- Preserve the approved full Replicator graphics, split/recombine behavior and established faction structure.
- Private historical builds are feature-coverage witnesses, not authority when they conflict with later user requirements.

This file is a migration checkpoint, not a completion claim.
